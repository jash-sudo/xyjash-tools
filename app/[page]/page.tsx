import {notFound} from 'next/navigation';import {Header} from '@/components/home-client';const copy:Record<string,[string,string]>={about:['About XYJASH Tools','A small, independent utility workbench made for the everyday jobs that should not require an account or a cluttered download.'],privacy:['Privacy Policy','XYJASH Tools does not sell personal information. Most operations run in your browser. Some network-backed tools send only the value needed to the named public endpoint.'],terms:['Terms of Use','Use these tools at your own discretion. Results are provided as-is and external services can be unavailable or rate-limited.'],contact:['Contact','For feedback, corrections, or tool requests, contact the site owner through the contact method configured for this domain.']};export default function Page({params}:{params:{page:string}}){const c=copy[params.page];if(!c)notFound();return <><Header/><main className="shell py-20"><p className="lime text-xs font-bold tracking-widest">XYJASH TOOLS</p><h1 className="mt-4 text-4xl font-black">{c[0]}</h1><p className="mt-6 max-w-2xl leading-8 muted">{c[1]}</p></main></>}
